<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SecureDoc - Home</title>
    <style>
        /* Modern Style Enhancements */
        * { 
            margin: 0; padding: 0; 
            box-sizing: border-box; 
            font-family: 'Inter', system-ui, -apple-system, sans-serif; 
        }
        
        body { 
            background: #f8faff; 
            min-height: 100vh;
            color: #2d3436;
        }

        /* Navigation Styling */
        nav { 
            background: linear-gradient(135deg, #1a73e8 0%, #0d47a1 100%);
            padding: 1rem 2rem; 
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            position: relative;
            z-index: 1000;
        }

        .nav-brand {
            display: flex;
            align-items: center;
            gap: 12px;
            color: white;
            font-size: 1.8rem;
            text-decoration: none;
            font-weight: 700;
        }

        .logo {
            width: 45px;
            height: 45px;
            background: url('logo-icon.png') no-repeat center/contain;
        }

        .nav-links { 
            display: flex; 
            gap: 2rem; 
            align-items: center;
        }

        .nav-link { 
            color: rgba(255,255,255,0.9); 
            text-decoration: none; 
            padding: 0.8rem 1.2rem; 
            border-radius: 8px; 
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .nav-link:hover { 
            background: rgba(255,255,255,0.15); 
            transform: translateY(-2px);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 6rem 0 4rem;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -10%;
            width: 120%;
            height: 200%;
            background: radial-gradient(circle at 50% 50%, rgba(26,115,232,0.1) 0%, transparent 60%);
            z-index: -1;
        }

        .hero h1 {
            font-size: 3.5rem;
            color: #1a1a1a;
            margin-bottom: 1.5rem;
            line-height: 1.2;
            font-weight: 800;
        }

        .hero h1 .highlight {
            color: #1a73e8;
            position: relative;
            display: inline-block;
        }

        .hero p {
            color: #5f6368;
            font-size: 1.3rem;
            margin-bottom: 3rem;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Feature Cards */
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 2rem 0;
            margin-top: 2rem;
        }

        .feature-card {
            background: white;
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            text-align: center;
            border: 1px solid rgba(0,0,0,0.05);
            position: relative;
            overflow: hidden;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 40px rgba(0,0,0,0.12);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent 40%, rgba(26,115,232,0.05) 100%);
            transform: rotate(45deg);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: #e8f1fd;
            border-radius: 12px;
            margin: 0 auto 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            color: #1a73e8;
        }

        .feature-card h3 {
            color: #1a1a1a;
            margin-bottom: 1rem;
            font-size: 1.4rem;
            font-weight: 700;
        }

        .feature-card p {
            color: #6b7280;
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        /* CTA Button */
        .cta-container {
            margin-top: 3rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 1rem 2rem;
            background: #1a73e8;
            color: white;
            text-decoration: none;
            border-radius: 10px;
            transition: all 0.3s ease;
            font-weight: 600;
            box-shadow: 0 4px 6px rgba(26,115,232,0.2);
        }

        .btn:hover {
            background: #1557b0;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(26,115,232,0.25);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .nav-brand { font-size: 1.4rem; }
            .hero h1 { font-size: 2.5rem; }
            .hero p { font-size: 1.1rem; }
            .features { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

<%- include('partials/header')%>

    <div class="container">
        <div class="hero">
            <h1>Secure Your Documents with <span class="highlight">SecureDoc</span></h1>
            <p>Advanced document management system with military-grade encryption and seamless collaboration features</p>
            
            <div class="cta-container">
                <a href="/documents" class="btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-right"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                    Get Started
                </a>
            </div>

            <div class="features">
                <div class="feature-card">
                    <div class="feature-icon">📁</div>
                    <h3>Secure Upload</h3>
                    <p>Advanced encryption for your sensitive documents with automatic version control</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🤝</div>
                    <h3>Smart Sharing</h3>
                    <p>Granular access controls with expiration dates and permission management</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🔒</div>
                    <h3>Military-Grade Security</h3>
                    <p>AES-256 encryption with 2FA protection for maximum security</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>